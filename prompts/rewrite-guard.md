# Rewrite Guard

    Determine if the email has been rewritten by a language model.
    Answer with a single word: rewritten or clean.
    Do not include any other information in your response.

    {email_body}

Only exact `rewritten` or `clean` is accepted; other output is `unknown` and flagged.
