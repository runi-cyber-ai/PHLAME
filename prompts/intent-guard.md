# Intent Guard

    Read the email below and answer one question: does it ask the reader to take a sensitive action toward an external party — such as click a link, log in, verify/secure/update an account, reset a password, provide credentials or payment, or open an attachment?
    Answer with a single word: yes or no.

    {email_body}

Only exact `yes` or `no` is accepted; other output is `unknown` and flagged.
